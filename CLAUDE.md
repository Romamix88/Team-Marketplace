# CLAUDE.md — Проект автоматизации «Атмосфера»

## О проекте

Компания «Атмосфера» (~70 человек) — импорт, производство и продажа текстиля (постельное бельё, подушки, одеяла). B2B — гостиницы, B2C — маркетплейсы (WB, Ozon, ЯМ, LaModa, Золотое яблоко) и собственный сайт.

Цель: инфраструктура взаимосвязанных AI-агентов, автономно выполняющих повторяющиеся операции в 1С:КА 2.5 и Битрикс24.

## Архитектура

### Системы компании
- **1С:КА 2.5** — склад, закупки, финансы, бухгалтерия, производство (сервер в офисе)
- **Битрикс24** — коммуникации, задачи, проекты, онлайн-встречи + переход CRM из 1С
- **Маркетплейсы** — WB, Ozon, ЯМ интегрированы с 1С (автосоздание заказов клиента)

### Стек
- **VPS-сервер**: вся инфраструктура агентов, MCP-серверов, дашборда — в одном месте
- **Связь VPS → 1С в офисе**: VPN (WireGuard) или прямое HTTPS-подключение к опубликованной базе 1С
- **LLM**: RouterAI (routerai.ru) — единый API ко всем моделям, оплата в рублях
- **Модели подбираются под задачу каждого агента**: Claude, GPT, DeepSeek, Gemini и др.

### VPS-сервер — технические параметры

**Стартовая конфигурация (Фазы 0-1):**
- CPU: 4 vCPU
- RAM: 8 ГБ
- SSD: 80 ГБ NVMe
- ОС: Ubuntu 24.04 LTS
- Сеть: статический IP, канал 200+ Мбит/с
- Docker Engine + Docker Compose

**Масштабированная конфигурация (Фазы 2-4, 6 агентов + все MCP):**
- CPU: 8 vCPU
- RAM: 16 ГБ
- SSD: 160 ГБ NVMe
- Остальное аналогично

**Провайдеры (рекомендуемые):**
- Россия: Timeweb Cloud, Selectel, VDSina
- Европа: Hetzner (дешевле, но данные за рубежом)

**Потребление ресурсов (оценка):**
- MCP-сервер 1С: ~256 МБ RAM, минимальный CPU
- MCP-сервер Битрикс24: ~256 МБ RAM, минимальный CPU
- Один агент: ~256-512 МБ RAM
- Дашборд: ~128 МБ RAM
- Логирование: ~256 МБ RAM
- Итого на 6 агентов + 2 MCP + дашборд + логи: ~4-5 ГБ RAM, остальное — запас

### Трёхуровневая архитектура
1. **Транспортный** — MCP-серверы (1С, Битрикс24), REST API — всё на VPS
2. **Логический** — AI-агенты с бизнес-логикой (RouterAI API) — на VPS
3. **Интерфейсный** — дашборд, уведомления в Битрикс24 — на VPS

### Связь VPS → 1С в офисе
MCP-сервер 1С стоит на VPS, но обращается к базе 1С в офисе. Варианты подключения:
- **Вариант A**: 1С опубликована на веб-сервере с HTTPS, доступна по белому IP. MCP-сервер обращается напрямую.
- **Вариант B**: WireGuard VPN-туннель между VPS и офисным роутером/сервером. MCP-сервер обращается к 1С по внутреннему адресу.
- Рекомендуется Вариант B (безопаснее — 1С не выставлена в интернет).

### MCP-серверы — модульная структура
Один MCP-сервер на систему, модули внутри:

**MCP-сервер 1С** (VPS, подключается к 1С в офисе через VPN):
- Модуль «Склад»: остатки, движения, скорость продаж
- Модуль «Закупки»: заказы поставщикам, заявки на оплату, статусы
- Модуль «Продажи»: счета, КП, история заказов, аналитика
- Модуль «Производство»: планы, статусы, потребность в сырье
- Модуль «Финансы»: сверка счетов/договоров, дебиторка/кредиторка, отчёты

**MCP-сервер Битрикс24** (VPS, подключается к облачному Битрикс24):
- Модуль «CRM»: сделки, контакты, лиды
- Модуль «Задачи»: создание, назначение, статусы
- Модуль «Коммуникации»: уведомления, чат-бот

Каждый агент видит только свой набор инструментов — конфигурация доступа.

### Связь MCP-сервера с 1С
- 1С публикуется на веб-сервере (Apache/IIS) в офисе
- HTTP-сервисы 1С — кастомные эндпоинты с бизнес-логикой
- OData REST — для быстрого доступа на чтение
- Сервисные пользователи: MCPAgent_ReadOnly (чтение), MCPAgent_Operator (запись)
- Доступ по HTTPS + Basic Auth

### RouterAI — выбор модели под агента
Единый API-ключ RouterAI, модель меняется одним параметром в конфиге агента.

| Агент | Рекомендуемая модель | Обоснование |
|---|---|---|
| Планировщик закупок | Claude Sonnet 4.6 / Claude Opus | Сложный анализ, сопоставление данных |
| Помощник продаж | Claude Sonnet 4.6 | Генерация КП, скоринг, работа с текстом |
| Помощник бухгалтерии | DeepSeek V3.2 | Структурированные сверки, дешевле |
| E-com монитор | DeepSeek V3.2 | Мониторинг, простая аналитика, дешевле |
| Документооборот | Claude Sonnet 4.6 | Генерация документов, работа с шаблонами |
| Оркестратор | GPT-5.4 / Claude Sonnet | Маршрутизация, координация |

Модели могут пересматриваться после тестирования — меняется одна строка в конфиге.

## Агенты

| ID | Агент | Фаза | Подразделение |
|---|---|---|---|
| agent-procurement | Планировщик закупок | 1 | Закупки, Производство, Склад |
| agent-sales | Помощник отдела продаж | 2 | Продажи (24 менеджера) |
| agent-accounting | Помощник бухгалтерии | 3 | Бухгалтерия (5 человек) |
| agent-ecom | E-com монитор | 3 | E-com (5 человек) |
| agent-docs | Документооборот | 4 | Все подразделения |
| agent-orchestrator | Оркестратор | 4 | Координация всех агентов |

## Фазы внедрения

- **Фаза 0** (2 нед): VPS + Docker. MCP-сервер 1С (модуль Склад, OData, чтение). MCP-сервер Битрикс24. VPN-туннель к офису. Дашборд.
- **Фаза 1** (3-4 нед): Агент «Планировщик закупок». Модуль Закупки (HTTP-сервисы, запись).
- **Фаза 2** (3-4 нед): Агент «Помощник продаж». Модуль Продажи. Параллельно с миграцией CRM в Битрикс24.
- **Фаза 3** (4 нед): Агенты «Бухгалтерия» и «E-com монитор». Модули Финансы.
- **Фаза 4** (2 нед): Оркестратор + документооборот.

## Правила разработки

### Безопасность
- **Human-in-the-loop**: агенты НЕ выполняют финансовых операций без подтверждения человека
- **Принцип минимальных привилегий**: агент видит только свои инструменты
- **Логирование**: каждое действие каждого агента записывается
- **Разграничение прав**: чтение всегда, запись — только для конкретных операций
- **Откат (rollback)**: при ошибках

### Структура проекта
```
atmosfera-agents/
├── CLAUDE.md              # Этот файл
├── CONTEXT.md             # Текущее состояние проекта
├── AGENTS.md              # Текстовый реестр агентов
├── docker-compose.yml     # Конфигурация всех контейнеров
├── mcp-1c/                # MCP-сервер 1С
│   ├── Dockerfile
│   ├── src/
│   │   ├── server.py      # Точка входа
│   │   ├── modules/
│   │   │   ├── warehouse.py   # Модуль Склад
│   │   │   ├── procurement.py # Модуль Закупки
│   │   │   ├── sales.py       # Модуль Продажи
│   │   │   ├── production.py  # Модуль Производство
│   │   │   └── finance.py     # Модуль Финансы
│   │   └── auth/
│   │       └── access.py      # Конфигурация доступа агентов
│   └── config.yml         # Подключение к 1С (URL, credentials)
├── mcp-bitrix/            # MCP-сервер Битрикс24
│   ├── Dockerfile
│   ├── src/
│   │   ├── server.py
│   │   └── modules/
│   │       ├── crm.py
│   │       ├── tasks.py
│   │       └── communications.py
│   └── config.yml
├── agents/                # AI-агенты
│   ├── procurement/       # Планировщик закупок
│   │   ├── Dockerfile
│   │   ├── agent.py
│   │   ├── config.yml     # Модель RouterAI, доступные инструменты
│   │   └── prompts/
│   ├── sales/             # Помощник продаж
│   ├── accounting/        # Помощник бухгалтерии
│   ├── ecom/              # E-com монитор
│   └── orchestrator/      # Оркестратор
├── dashboard/             # Веб-дашборд реестра агентов
│   ├── Dockerfile
│   └── src/
├── registry/              # Авто-реестр (heartbeat)
│   └── registry.json
└── logs/                  # Логирование
```

### Git-процесс
- Описательные коммиты на русском: `feat(mcp-1c/warehouse): добавлен инструмент получения остатков`
- Ветки по фазам: `phase-0/mcp-1c-warehouse`, `phase-1/agent-procurement`
- CONTEXT.md обновляется при каждом значимом изменении

### Стиль кода
- Python 3.11+ для MCP-серверов и агентов
- Type hints обязательны
- Docstrings на русском
- Конфигурация через YAML-файлы, секреты через переменные окружения
- Логирование через structlog
- Все вызовы LLM через RouterAI API (единый эндпоинт, модель указывается в config.yml агента)

## Контакты и доступы

- **1С тестовая база MCP**: atmosfera2-as.esit.info:18446
- **1С боевая база**: [будет добавлено]
- **Битрикс24 API**: [будет добавлено]
- **RouterAI API key**: [будет добавлено]
- **VPS**: [будет добавлено — IP, провайдер]
