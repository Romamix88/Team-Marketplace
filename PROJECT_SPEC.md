# Техническое задание: Автоматизация компании «Атмосфера»

## Инфраструктура AI-агентов на базе 1С:КА 2.5 + Битрикс24

Версия: 3.0 | Дата: 01 апреля 2026

---

## 1. Базы 1С

### 1.1. Боевая база 1С:КА 2.5
- Расположена на сервере в офисе компании
- Публикация на веб-сервере (Apache или IIS) с доступом по HTTPS
- Сервисные пользователи:
  - **MCPAgent_ReadOnly** — только чтение справочников, регистров, документов
  - **MCPAgent_Operator** — чтение + создание определённых документов
- Используется для готовых, протестированных решений

### 1.2. Тестовая база 1С:КА 2.5
- Актуальная копия боевой базы (регулярная синхронизация)
- Аналогичная публикация на веб-сервере
- Аналогичные сервисные пользователи
- Тестовый MCP-сервер уже подключён: atmosfera2-as.esit.info:18446

---

## 2. HTTP-сервисы 1С

### 2.1. Назначение
HTTP-сервисы — это «окошки» в базе 1С, через которые MCP-серверы запрашивают данные и создают документы. Работают с живыми данными в реальном времени, ничего не копируют.

### 2.2. Два типа доступа

**OData REST (быстрый старт, только чтение):**
- Публикация стандартных объектов 1С через встроенный механизм
- Не требует программирования в 1С
- Настройка: конфигуратор → Администрирование → Публикация на веб-сервере

**Кастомные HTTP-сервисы (для операций записи):**
- Разрабатываются программистом 1С
- Содержат бизнес-логику: проверки, заполнение полей, проведение документов
- Объём: 30-50 строк на инструмент чтения, 100-200 строк на инструмент записи

### 2.3. Необходимые HTTP-сервисы по фазам

**Фаза 0 — модуль «Склад» (OData, чтение):**
- Остатки по номенклатуре / складу
- История движений товаров
- Скорость продаж по позициям (B2B + B2C)
- Справочник номенклатуры
- Справочник контрагентов

**Фаза 1 — модуль «Закупки» (HTTP-сервисы, запись):**
- Создание заказа поставщику
- Создание заявки на оплату
- Статусы оплат и поставок
- История закупок по поставщику

**Фаза 2 — модуль «Продажи» (HTTP-сервисы, чтение + запись):**
- Создание счёта клиенту
- История заказов / сделок клиента
- Аналитика продаж (B2B / B2C)
- Данные для КП

**Фаза 3 — модуль «Финансы» (HTTP-сервисы, чтение):**
- Сверка счетов и договоров
- Дебиторская / кредиторская задолженность
- Данные для отчётов

### 2.4. Задание для специалиста по 1С
«Нам нужно опубликовать базу на веб-сервере с доступом через REST API. На первом этапе — OData для чтения. На втором — кастомные HTTP-сервисы для создания документов. Нужны сервисные пользователи с ограниченными правами. Спецификация по каждому эндпоинту будет предоставлена отдельно.»

---

## 3. VPS-сервер

### 3.1. Единый сервер для всей инфраструктуры
Вся инфраструктура агентов размещается на одном VPS-сервере: MCP-серверы, агенты, дашборд, логирование. Отдельный сервер в офисе не требуется.

### 3.2. Стартовая конфигурация (Фазы 0-1)
- **CPU**: 4 vCPU
- **RAM**: 8 ГБ
- **Диск**: 80 ГБ NVMe SSD
- **ОС**: Ubuntu 24.04 LTS
- **Сеть**: статический IPv4, канал 200+ Мбит/с
- **ПО**: Docker Engine, Docker Compose, WireGuard

### 3.3. Масштабированная конфигурация (Фазы 2-4)
- **CPU**: 8 vCPU
- **RAM**: 16 ГБ
- **Диск**: 160 ГБ NVMe SSD
- Остальное аналогично

### 3.4. Оценка потребления ресурсов
| Компонент | RAM | CPU |
|---|---|---|
| MCP-сервер 1С | ~256 МБ | минимальный |
| MCP-сервер Битрикс24 | ~256 МБ | минимальный |
| Один AI-агент | ~256-512 МБ | минимальный |
| Дашборд | ~128 МБ | минимальный |
| Система логирования | ~256 МБ | минимальный |
| **Итого (6 агентов + всё)** | **~4-5 ГБ** | **2-3 vCPU** |

Остальные ресурсы — запас на пиковые нагрузки и рост.

### 3.5. Рекомендуемые провайдеры
- **Timeweb Cloud** — российский, поддержка на русском, оплата в рублях
- **Selectel** — российский, надёжный, хорошая сеть
- **VDSina** — российский, гибкие тарифы
- **Hetzner** — немецкий, дешевле, но данные за рубежом

### 3.6. Что размещается на VPS
- Docker-окружение со всеми контейнерами
- MCP-сервер 1С (подключается к 1С в офисе через VPN)
- MCP-сервер Битрикс24 (подключается к облачному Битрикс24)
- Все AI-агенты (обращаются к RouterAI API)
- Веб-дашборд реестра агентов
- Система логирования
- WireGuard VPN-сервер (туннель к офису)

### 3.7. Ориентировочная стоимость
- Стартовая конфигурация: ~1 500-3 000 ₽/мес
- Масштабированная: ~3 000-6 000 ₽/мес

---

## 4. Docker-окружение

### 4.1. Назначение
Каждая программа работает в изолированном контейнере. Один упал — остальные работают. Обновление одного компонента не затрагивает другие.

### 4.2. Компоненты
- Docker Engine + Docker Compose
- Файл `docker-compose.yml` описывает все контейнеры
- Запуск: `docker compose up -d`
- Остановка: `docker compose down`
- Обновление одного сервиса: `docker compose up -d --build mcp-1c`

---

## 5. Связь VPS → 1С в офисе

### 5.1. Проблема
MCP-сервер 1С стоит на VPS, а база 1С — в офисе. Нужен безопасный канал связи.

### 5.2. Рекомендуемое решение — WireGuard VPN
- На VPS — WireGuard-сервер
- На офисном сервере/роутере — WireGuard-клиент
- Зашифрованный туннель, 1С не выставлена в интернет
- MCP-сервер обращается к 1С по внутреннему адресу (например, 10.0.0.2)
- Настройка: ~30 минут для сисадмина

### 5.3. Альтернатива — прямая публикация
- 1С опубликована по HTTPS с белым IP
- MCP-сервер обращается напрямую
- Проще, но менее безопасно (1С видна в интернете)

### 5.4. Схема сети
```
[Офис]                              [VPS]
  Сервер 1С                          MCP-сервер 1С
  Веб-сервер (Apache/IIS)            MCP-сервер Битрикс24
  WireGuard-клиент ◄── туннель ──► WireGuard-сервер
  10.0.0.2                           Агенты (x6)
                                     Дашборд
                                     Логирование
                                     10.0.0.1
```

---

## 6. MCP-сервер 1С

### 6.1. Назначение
Прослойка-переводчик между агентами и базой 1С. Не хранит данных — каждый запрос обращается к живой базе через HTTP-сервисы 1С.

### 6.2. Размещение
Docker-контейнер на VPS. Подключается к 1С в офисе через WireGuard VPN.

### 6.3. Модульная структура
Один сервер, модули внутри (отдельные файлы). Модули добавляются по фазам.

### 6.4. Тройная защита
1. Права пользователя в 1С
2. Набор инструментов MCP-сервера
3. Конфигурация доступа агентов

---

## 7. MCP-сервер Битрикс24

### 7.1. Размещение
Docker-контейнер на VPS. Подключается к облачному Битрикс24 через REST API.

### 7.2. Подключение
REST API Битрикс24 (webhook или OAuth 2.0). Нужны API-ключи от администратора.

---

## 8. AI-агенты

### 8.1. Размещение
Docker-контейнеры на VPS. Каждый агент — отдельный контейнер.

### 8.2. LLM-провайдер — RouterAI (routerai.ru)
- Единый API-ключ для всех моделей
- Модель указывается в конфиге каждого агента
- Переключение модели — изменение одной строки
- Автоматическая отказоустойчивость
- Оплата в рублях (карта или счёт для юрлиц)
- Инфраструктура RouterAI — в РФ

### 8.3. Выбор модели под агента
| Агент | Модель | Почему |
|---|---|---|
| Планировщик закупок | Claude Sonnet 4.6 | Сложный анализ данных, сопоставления |
| Помощник продаж | Claude Sonnet 4.6 | Генерация КП, скоринг, текстовая работа |
| Помощник бухгалтерии | DeepSeek V3.2 | Структурированные сверки, экономия |
| E-com монитор | DeepSeek V3.2 | Простой мониторинг и алерты, экономия |
| Документооборот | Claude Sonnet 4.6 | Генерация документов |
| Оркестратор | GPT-5.4 | Маршрутизация и координация |

### 8.4. Ориентировочная стоимость RouterAI
- Пилот (1-2 агента): 3 000-10 000 ₽/мес
- Полное развёртывание (6 агентов): 15 000-40 000 ₽/мес
- Точная оценка — после пилота Фазы 1

---

## 9. Онлайн-дашборд

### 9.1. Назначение
Веб-интерфейс с реальным статусом всех компонентов: агенты, MCP-серверы, связи, ошибки.

### 9.2. Размещение
Docker-контейнер на VPS. Доступен из браузера по URL с авторизацией.

### 9.3. Автообновление
Каждый компонент при запуске регистрируется в `registry.json` (heartbeat). Дашборд отображает актуальное состояние автоматически.

---

## 10. RouterAI — аккаунт и API

### 10.1. Что нужно
- Регистрация на routerai.ru
- Для юрлица: заключение договора, оплата по счёту
- Получение API-ключа
- Настройка лимитов расходов (если нужно)

### 10.2. Преимущества для проекта
- Не нужны отдельные аккаунты Anthropic, OpenAI, Google
- Единый биллинг, прозрачная аналитика расходов по моделям
- Если одна модель недоступна — автоматическое переключение на резерв
- Смена модели агента — одна строка в конфиге, без изменения кода

---

## 11. Система логирования

### 11.1. Что логируется
- Каждый вызов MCP-инструмента: кто, что, когда, результат
- Каждое решение агента: данные на входе, вывод, действие
- Каждая операция записи: что создал/изменил в 1С или Битрикс24
- Ошибки, таймауты, отказы

### 11.2. Размещение
Docker-контейнер на VPS.

---

## 12. Механизм human-in-the-loop

### 12.1. Принцип
Любая операция записи требует подтверждения ответственного сотрудника.

### 12.2. Как работает
Агент формирует действие → уведомление в Битрикс24 (или Telegram) → сотрудник подтверждает/отклоняет → только после подтверждения агент выполняет действие → всё логируется.

### 12.3. Эволюция
Начало: подтверждение для ВСЕХ операций записи. Со временем: автоматизация рутинных операций ниже порога (например, заказы до 50 000 ₽ — автоматически).

---

## Порядок запуска проекта

### Подготовительные действия (до Фазы 0)
1. Арендовать VPS (4 vCPU, 8 ГБ RAM, 80 ГБ SSD, Ubuntu 24.04)
2. Установить Docker + Docker Compose на VPS
3. Настроить WireGuard VPN между VPS и офисом
4. Зарегистрироваться на routerai.ru, получить API-ключ
5. Получить API-ключи Битрикс24
6. Поставить задачу специалисту 1С:
   - Сервисные пользователи + публикация базы на веб-сервере
   - Спецификация HTTP-сервисов (предоставим)

### Фаза 0 — Фундамент (2 недели)
1. Аудит тестового MCP-сервера 1С
2. Спецификация HTTP-сервисов модуля «Склад» (ТЗ для 1С)
3. Программист 1С: OData + HTTP-сервисы модуля «Склад»
4. Разработка MCP-сервера 1С (модуль «Склад»)
5. Разработка MCP-сервера Битрикс24
6. Тестирование на тестовой базе
7. Переключение на боевую (только чтение)
8. Развёртывание дашборда

### Фаза 1 — Пилот: Закупки (3-4 недели)
1. HTTP-сервисы модуля «Закупки» (программист 1С)
2. Расширение MCP-сервера 1С (модуль «Закупки»)
3. Разработка агента «Планировщик закупок» (модель: Claude Sonnet 4.6)
4. Настройка human-in-the-loop
5. Тестирование → пилот → итерация

### Фазы 2-4 — по аналогии
